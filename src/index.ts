import waterService, {
  WaterService,
  requestType,
  responseFilters,
  ConfDataBase
} from "./core";

export { WaterService, requestType, responseFilters, ConfDataBase };
export default waterService;
